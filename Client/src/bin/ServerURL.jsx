import React from "react";

const ServerURL = () => {
  const serverurl = "http://localhost:1337/";
  console.log(serverurl);

  return <div>Server URL: {serverurl}</div>;
};

export default ServerURL;
